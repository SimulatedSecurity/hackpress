# Vulnerability validation templates

Validation templates use the same JSON format as exploit templates (safe / read-only intent).

- Full guide: **[TEMPLATES.md](TEMPLATES.md)**
- Single: `hackpress vuln <url> --template <path>`
- Mass: `hackpress vuln <url> --template-dir <dir> --threads 20`
- Examples: `templates/vulns/`
